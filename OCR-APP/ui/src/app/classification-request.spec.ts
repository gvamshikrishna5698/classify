import { ClassificationRequest } from './classification-request';

describe('ClassificationRequest', () => {
  it('should create an instance', () => {
    expect(new ClassificationRequest()).toBeTruthy();
  });
});
