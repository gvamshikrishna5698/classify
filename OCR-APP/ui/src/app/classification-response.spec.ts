import { ClassificationResponse } from './classification-response';

describe('ClassificationResponse', () => {
  it('should create an instance', () => {
    expect(new ClassificationResponse()).toBeTruthy();
  });
});
